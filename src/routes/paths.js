// One place for every route path. Screens and redirects import from here so a
// renamed route is a single edit, never a hunt for string literals.
export const PATHS = {
  start: "/",
  quiz: "/quiz",
  result: "/result",
};
